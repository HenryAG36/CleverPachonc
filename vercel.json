{
  "buildCommand": "cd frontend && npm install && npm run build && cp -r dist ../api/static",
  "functions": {
    "api/index.py": { "maxDuration": 60 }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index.py" }
  ]
}
